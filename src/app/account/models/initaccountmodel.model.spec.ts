import { Initaccountmodel } from './initaccountmodel.model';

describe('Initaccountmodel', () => {
  it('should create an instance', () => {
    expect(new Initaccountmodel()).toBeTruthy();
  });
});
